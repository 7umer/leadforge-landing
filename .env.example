# The deployed LeadForge app's origin. Every CTA on this page ("Start free",
# "Get Pro", "Log in", ...) links to <this>/register or <this>/login.
# Falls back to the live app in src/Landing.jsx if unset, so this only needs
# setting to point a build somewhere else (e.g. a staging app).
VITE_APP_URL=https://leadforge-9r0v.onrender.com
